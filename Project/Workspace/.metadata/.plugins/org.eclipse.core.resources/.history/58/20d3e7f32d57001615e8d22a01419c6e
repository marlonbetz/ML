import numpy as np 
from keras.models import Model
from keras.layers import LSTM,Dense,Embedding,Input

length = 10
n_symbols = 1000
dim_embedding = 10
input = Input((1,))
embedded = Embedding(n_symbols, 64, input_length=10)